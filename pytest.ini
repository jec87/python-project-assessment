[pytest]
# Look for tests in the tests directory.
testpaths = tests

# Allow tests to import project modules from the repository root.
pythonpath = .

# Include a summary of all results except passed tests.
addopts = -ra
